fn main() {
    let a = 10;
    let mut b = 20;
    
    // a += 1; // Proibido: a imutável
    b += 1; // Permitido: b mutável
    
    // Quando o código termina aqui, a e b são liberados da memória
}

